# Scenarios

Scenarios assemble processing stages into an executable mode. The active scenario is selected by name from launch config through `ScenarioFactory`.

Current scenario names:

- `nn_manual`
- `nn_auto`
- `opencv_manual`
- `opencv_auto_motion`
