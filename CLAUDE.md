# Claude Project Entry: Thermal Tracker

This file is the short entry point for Claude Code.

Before any project work, read:

@docs_llm/llm_index.md

Follow the document routing and task rules from `docs_llm/llm_index.md`.

Do not change code unless the current task document explicitly allows it.
Do not run `git push`.
